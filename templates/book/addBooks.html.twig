<h1>Ajouter un Livre</h1>

{{ form_start(form) }}
{{ form_widget(form) }}
<button type="submit" class="btn btn-primary">Ajouter livre</button>
{{ form_end(form) }}
<a href="{{ path('app_listBook') }}" class="btn btn-secondary">Retour à la liste</a>
